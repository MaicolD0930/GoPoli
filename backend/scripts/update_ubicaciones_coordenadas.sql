-- OPCIONAL: solo si alguna ubicación no recibió coordenadas al arrancar el backend.
-- GoPoli asigna lat/long automáticamente por nombre (UbicacionCoordenadasSeeder.java).
-- Usa esto solo para corregir a mano o nombres muy raros:

-- UPDATE ubicacion SET latitud = 6.3381, longitud = -75.3785 WHERE nombre_ubicacion ILIKE '%Niquía%';
